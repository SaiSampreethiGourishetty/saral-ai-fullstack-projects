# Simple Task App

A minimal full stack project with FastAPI backend and plain HTML/JS frontend.

## Features
- Add tasks
- View tasks
- REST API with FastAPI
- Simple frontend

## Run
1. Install requirements: `pip install -r requirements.txt`
2. Start backend: `uvicorn backend:app --reload`
3. Open `index.html` in browser
